import React from 'react';
class Albums extends React.Component {
    // Defining and Returning Albums Component
    render() {
        return (
            <div className="albums">
            </div>
        );
    }
}

export default Albums;